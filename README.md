# cr1labDarinIT15

# Задача 1

1. Код разделен на 3 файла:
- ArtifactSolver.h
- ArtifactSolver.cpp
- main.cpp

2. Исправления:
- исправлено условие задачи: суммарный вес должен быть больше Z, а не больше или равен Z
- добавлена проверка открытия файла
- добавлена проверка чтения входных данных
- исправлен вывод результата: теперь выводится налоговое бремя

3. Переименования:
- knapSack -> SolveTask
- wt -> Weights
- val -> Taxes
- selected -> Chosen
- bestSelection -> Result.Items
- minVal -> BestTax

4. Дополнительно:
- чтение данных вынесено в отдельную функцию
- вывод результата вынесен в отдельную функцию
- добавлены комментарии FIX_ME в местах исправлений

# Задача 2

1. Код разделен на 3 файла:
- KingSolver.h
- KingSolver.cpp
- main.cpp

2. Исправления:
- добавлена проверка открытия входного файла
- добавлена проверка открытия выходного файла
- добавлена проверка корректности размера доски
- добавлена проверка чтения элементов доски
- исправлено и упрощено восстановление пути
- уточнена логика направлений L и U

3. Переименования:
- docka -> Board
- dp -> Dp
- maxSum -> Result.MaxSum
- path -> Result.Path

4. Дополнительно:
- чтение данных вынесено в отдельную функцию
- решение задачи вынесено в отдельную функцию
- запись результата вынесена в отдельную функцию
- добавлены комментарии FIX_ME

# Задача 3

1. Код разделен на 3 файла:
- KNumbers.h
- KNumbers.cpp
- main.cpp

2. Исправления:
- исправлена ошибка первого разряда: число больше не может начинаться с нуля
- исправлен итоговый ответ: теперь выводится количество, а не доля
- убран лишний вывод из вычисляющей функции
- добавлена проверка ограничений входных данных

3. Переименования:
- Count -> SolveTask
- dp -> Dp
- count -> Result.Count
- HomeDyn16 -> удалена как лишняя

4. Дополнительно:
- решение вынесено в отдельную функцию
- вывод вынесен в отдельную функцию
- добавлены комментарии FIX_ME

# Задача 4

1. Код разделен на 3 файла:
- ExpressionSolver.h
- ExpressionSolver.cpp
- main.cpp

2. Исправления:
- убраны циклы, так как по условию они запрещены
- удалена поддержка скобок и умножения, так как они не входят в грамматику задачи
- упрощен разбор выражения: оставлены только цифры и операции +/-
- убран лишний текст из консольного вывода

3. Переименования:
- calc -> Calc
- str -> Str
- r -> Result

4. Дополнительно:
- решение полностью переведено на рекурсию
- добавлены комментарии FIX_ME
