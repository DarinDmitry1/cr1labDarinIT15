# cr1labDarinIT15

Лабораторная работа 1

Что было неправильно:

Код не был разделён на файлы (main, header, cpp).

Использовался using namespace std.

Названия переменных и функций были не в едином стиле.

Логика чтения файла, вычислений и вывода находилась в одном месте.

Было мало комментариев.

Не было нормальной обработки ошибки открытия файла.

Что исправлено:

Код разделён на файлы.

Убран using namespace std.

Приведены к единому стилю имена функций и переменных.

Логика программы разделена на отдельные функции.

Добавлены комментарии.

Улучшена проверка входного файла.

Лабораторная работа 2

Что было неправильно:

Код не был разделён на файлы (main, header, cpp).

Использовался using namespace std.

Были не самые удачные названия переменных.

Вся логика находилась в main.

Почти отсутствовали комментарии.

Не было проверки открытия файлов.

Что исправлено:

Код разделён на файлы.

Убран using namespace std.

Переименованы переменные и функции в более понятный вид.

Основная логика вынесена из main в отдельные функции.

Добавлены комментарии.

Добавлена проверка входного и выходного файлов.

Лабораторная работа 3

Что было неправильно:

Условие требовало ввод и вывод через файлы, а программа работала через консоль.

Код не был разделён на файлы (main, header, cpp).

Использовался using namespace std.

В программе смешаны вычисления и ввод/вывод.

Было мало комментариев.

Названия некоторых функций были не очень понятными.

Что исправлено:

Ввод и вывод переделаны через файлы.

Код разделён на файлы.

Убран using namespace std.

Логика вычислений вынесена в отдельные функции.

Добавлены комментарии.

Имена функций и переменных приведены к более аккуратному стилю.

Лабораторная работа 4

Что было неправильно:

Решение не соответствовало условию задачи.

Использовались лишние конструкции: циклы, умножение, скобки.

Программа решала более общую задачу, чем требовалось.

Код не был разделён на файлы.

Использовался using namespace std.

Было мало комментариев.

Что исправлено:

Код приведён в соответствие с условием задачи.

Удалены лишние операции и конструкции.

Убраны циклы.

Код разделён на файлы (main, header, cpp).

Добавлены комментарии.

Стиль кода приведён к более аккуратному виду.
